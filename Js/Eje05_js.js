let f = () => {console.log('Funpcion flecha'); }
let f1 = (x) => x*x;
let f2 = (x,y,z) => {return x + y + z; }

console.log( f() );
console.log( f1(4) );
console.log( f2(3, 5, 7) );

 alert('Esta es una alerta informativa');
 let valor = prompt('Dame la cantidad', '');
 console.log(typeof valor);

 let confirmar('¿Seguro de Eliminar?'):
 console.log(confirmar);
 
 if (!confirmar)
 console.log('Se eliminó');
 else
    console.log('El usuario canceló');
 
 window.onload = getEdad;

 function getEdad(){
     var edad = prompt('Introduce tu edad', '');
     if (isNaN(edad)) {
         alert('Intruce un número válido');
     else {
         if (Number)
     }
     }
 }

  function getColor() {varcolor= prompt('¿Cuál es tu color favorito?', '');switch(color) {case'rojo’:alert('El color rojo es una buena opción!’);break;case'negro’:alert('El color negro combina con todo!’);break;case'blanco’:alert('Es genial este color’);break;default:alert('El color:'+ color+ ' no está en la lista’);break;}}
 